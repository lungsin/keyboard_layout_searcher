#include <bits/stdc++.h>

#include <iostream>

using namespace std;

int main(int, char **) {
  std::cout << "Hello, from klungsyzer!\n";

  optional<reference_wrapper<int>> optionalRef;

  return 0;
}